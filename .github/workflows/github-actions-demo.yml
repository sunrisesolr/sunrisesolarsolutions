name: github actions demo
on: [push]
jobs:
  build-job:
    runs-on:
      ubuntu-latest
    steps:
      - name: check out repo code
        uses: actions/checkout@v4
      - run: just build
        
